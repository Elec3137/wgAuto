[Unit]
Description=simple wireguard autostart script

[Service]
User=root
WorkingDirectory=/bin/
ExecStart=/bin/e_wgAuto start [netname] [privkey] [pubkey] [endpoint] [port] [addre] [nm]
# Restart=always
# RestartSec=3

[Install]
WantedBy=multi-user.target

